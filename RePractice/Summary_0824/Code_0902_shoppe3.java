package RePractice.Summary_0824;

public class Code_0902_shoppe3 {
    public int expect (int n) {
        return (int)((-2) * (n * - Math.pow(0.5,n) + Math.pow(0.5,n) * (1 - Math.pow(0.5,n))));
        return  (int)(Math.random() % 1000);
    }
}
